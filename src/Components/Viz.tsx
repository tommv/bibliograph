import React, { FC } from "react";
import Graph from "graphology";

import "./Viz.css";

const Viz: FC<{ graph: Graph; onGoBack(): void }> = () => {
  return <section className="Viz">TODO</section>;
};

export default Viz;
